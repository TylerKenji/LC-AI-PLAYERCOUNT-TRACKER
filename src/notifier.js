const notifier = {
    sendNotification: (message) => {
        // Implement your notification logic here
        // This could be sending an email, a message to a chat service, etc.
        console.log(message);
    }
};

export default notifier;