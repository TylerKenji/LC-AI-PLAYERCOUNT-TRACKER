{
  "name": "steam-limbus-ai-notifier",
  "version": "1.0.0",
  "description": "An AI agent that notifies when the online player count for Limbus Company reaches an all-time new high.",
  "main": "src/index.js",
  "type": "module",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js",
    "test": "echo \"No tests specified yet\" && exit 0",
    "setup": "npm install && echo \"Please copy .env.example to .env and configure your Steam API key\"",
    "check-deps": "npm audit"
  },
  "dependencies": {
    "axios": "^1.10.0",
    "dotenv": "^10.0.0"
  },
  "author": "Your Name",
  "license": "MIT"
}
